{
  "name": "gdrive_archiver",
  "version": "1.0.1",
  "description": "A utility to search for `.archive.7z` files and move them to the Google Drive `Archive` folder.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Camilleri",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^73.0.0",
    "nodemailer": "^6.6.0"
  }
}
